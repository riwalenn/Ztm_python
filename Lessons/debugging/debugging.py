# Lessons
# - debugging :
#    - linting
#    - ide / editor
#    - read errors
#    - pdb (python debugger, used to step through the code and debug) (use import pdb)
#       pdb.set_trace()
#       the interaction is in the terminal (Pdb) what I want to debug (ex: num1 or num2 in a method)
#       you can use help (ex: help list)
#    -
